# Raspberry Pi 3/4 boot configuration

# 64-bit mode
arm_64bit=1

kernel=u-boot.bin

enable_uart=1

disable_overscan=1

# GPU memory size for each total memory size
gpu_mem_256=100
gpu_mem_512=100
gpu_mem_1024=100
